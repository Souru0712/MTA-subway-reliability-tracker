services:
  # ── Kafka (Redpanda) ────────────────────────────────────────────────────────
  redpanda:
    image: redpandadata/redpanda:v24.1.1
    container_name: redpanda
    command:
      - redpanda
      - start
      - --kafka-addr internal://0.0.0.0:9092,external://0.0.0.0:19092
      - --advertise-kafka-addr internal://redpanda:9092,external://${REDPANDA_ADVERTISE_ADDR:-localhost}:19092
      - --pandaproxy-addr internal://0.0.0.0:8083,external://0.0.0.0:18083
      - --advertise-pandaproxy-addr internal://redpanda:8083,external://localhost:18083
      - --schema-registry-addr internal://0.0.0.0:8081,external://0.0.0.0:18081
      - --rpc-addr redpanda:33145
      - --advertise-rpc-addr redpanda:33145
      - --mode dev-container
      - --smp 1
      - --default-log-level=warn
      - --memory 536870912
      - --reserve-memory 0
      - --set redpanda.default_topic_partitions=16
    ports:
      - "9092:9092"   # Kafka API
      - "9644:9644"     # Redpanda admin API
    volumes:
      - redpanda-data:/var/lib/redpanda/data
    healthcheck:
      test: ["CMD", "rpk", "cluster", "info"]
      interval: 15s
      timeout: 10s
      retries: 5

  redpanda-console:
    image: redpandadata/console:v2.7.2
    container_name: redpanda-console
    ports:
      - "8082:8080"
    environment:
      CONFIG_FILEPATH: /etc/redpanda/redpanda-console-config.yaml
    volumes:
      - ./infra/redpanda/console-config.yaml:/etc/redpanda/redpanda-console-config.yaml:ro
    depends_on:
      redpanda:
        condition: service_healthy

  # ── Topic Init ──────────────────────────────────────────────────────────────
  redpanda-init:
    image: redpandadata/redpanda:v24.1.1
    container_name: redpanda-init
    depends_on:
      redpanda:
        condition: service_healthy
    entrypoint: ["/bin/bash", "-c"]
    command:
      - |
        rpk topic create mta.trip_updates      --partitions 16 --brokers redpanda:9092 || true
        rpk topic create mta.vehicle_positions --partitions 16 --brokers redpanda:9092 || true
        rpk topic create mta.alerts            --partitions 16 --brokers redpanda:9092 || true

        rpk topic alter-config mta.trip_updates      --set retention.bytes=8589934592  --set retention.ms=604800000 --brokers redpanda:9092
        rpk topic alter-config mta.vehicle_positions --set retention.bytes=1610612736  --set retention.ms=604800000 --brokers redpanda:9092
        rpk topic alter-config mta.alerts            --set retention.bytes=536870912   --set retention.ms=604800000 --brokers redpanda:9092

        echo "Topics ready:"
        rpk topic list --brokers redpanda:9092
    restart: "no"

  # ── Spark ───────────────────────────────────────────────────────────────────
  spark-master:
    build:
      context: .
      dockerfile: infra/docker/Dockerfile.spark
    container_name: spark-master
    entrypoint: ["/opt/spark/bin/spark-class", "org.apache.spark.deploy.master.Master"]
    ports:
      - "7077:7077"
      - "8081:8080"   # Spark master web UI
      - "4040:4040"   # Spark application UI (active while spark-submit runs)
    volumes:
      - ./src:/opt/spark/work/src
      - ./config:/opt/spark/work/config
    env_file:
      - .env
    environment:
      KAFKA_BOOTSTRAP_SERVERS: redpanda:9092

  spark-worker:
    build:
      context: .
      dockerfile: infra/docker/Dockerfile.spark
    container_name: spark-worker
    entrypoint: ["/opt/spark/bin/spark-class", "org.apache.spark.deploy.worker.Worker", "spark://spark-master:7077"]
    environment:
      SPARK_WORKER_MEMORY: 1g
      SPARK_WORKER_CORES: "1"
      KAFKA_BOOTSTRAP_SERVERS: redpanda:9092
    depends_on:
      - spark-master
    volumes:
      - ./src:/opt/spark/work/src
      - ./config:/opt/spark/work/config
    env_file:
      - .env

  # ── GTFS Producer ───────────────────────────────────────────────────────────
  gtfs-producer:
    build:
      context: .
      dockerfile: infra/docker/Dockerfile.producer
    container_name: gtfs-producer
    env_file:
      - .env
    environment:
      KAFKA_BOOTSTRAP_SERVERS: redpanda:9092
    depends_on:
      redpanda:
        condition: service_healthy
      redpanda-init:
        condition: service_completed_successfully
    restart: on-failure

volumes:
  redpanda-data:
