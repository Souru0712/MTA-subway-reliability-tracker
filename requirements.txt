# GTFS / Kafka
gtfs-realtime-bindings==2.0.0
confluent-kafka==2.3.0
requests==2.31.0

# Spark (install PySpark separately via pip for local dev)
pyspark==3.5.0

# Postgres
psycopg2-binary==2.9.9
asyncpg==0.29.0

# AWS / S3
boto3==1.34.0
pyarrow==15.0.0

# Snowflake
snowflake-connector-python==3.7.0

# Airflow (pin version to avoid resolver conflicts)
apache-airflow==2.8.1
apache-airflow-providers-snowflake==5.3.0
apache-airflow-providers-amazon==8.15.0

# FastAPI
fastapi==0.110.0
uvicorn[standard]==0.27.0
pydantic==2.6.0

# Config / utilities
python-dotenv==1.0.1
pandas==2.2.0

# Testing
pytest==9.0.3
pytest-asyncio==1.3.0
httpx==0.27.0
email-validator>=2.0
